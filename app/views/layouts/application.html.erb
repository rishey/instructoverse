<!DOCTYPE html>
<html>
<head>
  <title>Instructoverse</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>
<!-- following user-widget is for oauth  -->
<div id="user-widget">
  <% if current_user %>
    Welcome <strong><%= current_user.name %></strong>!
    
    <%= link_to "Sign out", logout_path, id: "sign_out" %>
  <% else %>
    <%= link_to "Sign in with Facebook", "/auth/facebook", id: "sign_in" %>
  <% end %>
</div>

  <div class="top-nav">
  <h1 class="site-name">Instructoverse</h1>
    <nav>
      <ul>
        <% if current_user %>
        <li> Welcome <%= current_user.name %><%= link_to "Sign out", logout_path, id: "sign_out" %></li>   
        <li> <%= link_to "My Profile", user_path(current_user) %> </li>
        <% else %> 
        <li> <%= link_to "Sign in with Facebook", "/auth/facebook", id: "sign_in" %></li>
        <% end %>
        <li> <%= link_to "All courses", courses_path %> </li>
      </ul>
    </nav>
  </div>


  <div class="container">


<%= yield %>
  <div>
</body>
</html>
